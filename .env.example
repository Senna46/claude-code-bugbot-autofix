# ============================================================
# Bugbot Autofix Configuration
# ============================================================

# GitHub organizations to monitor (comma-separated)
# All repos under these orgs will be scanned for Cursor Bugbot reports
# NOTE: Quote values that contain commas (required by dotenv v17)
# Example: AUTOFIX_GITHUB_ORGS="my-org,another-org"
AUTOFIX_GITHUB_ORGS=

# Specific GitHub repos to monitor (comma-separated, format: owner/repo)
# Example: AUTOFIX_GITHUB_REPOS="my-org/repo-a,my-org/repo-b"
AUTOFIX_GITHUB_REPOS=

# Polling interval in seconds (default: 120)
AUTOFIX_POLL_INTERVAL=120

# Working directory for cloned repos (default: ~/.bugbot-autofix/repos)
AUTOFIX_WORK_DIR=

# SQLite database path (default: ~/.bugbot-autofix/state.db)
AUTOFIX_DB_PATH=

# Claude model override (optional, uses default if empty)
AUTOFIX_CLAUDE_MODEL=

# Log level: debug, info, warn, error (default: info)
AUTOFIX_LOG_LEVEL=info

# ============================================================
# Authentication
# ============================================================

# GitHub token (required on macOS Docker, optional on Linux if gh CLI is authenticated)
GH_TOKEN=

# Claude Code OAuth token (required on macOS Docker)
# Generate with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Or use Anthropic API key for pay-as-you-go
ANTHROPIC_API_KEY=
